---
description: Take up the objective this project left open in an earlier session
allowed-tools: Bash(node:*)
---

!`node "${CLAUDE_PLUGIN_ROOT}/bin/bandaid.js" goal adopt`

Relay that verbatim, then verify current state before assuming any of it is
already done. The criteria, constraints and blockers come across exactly as they
were; the continuation budget is fresh. Evidence recorded in an earlier session
is a claim about an earlier worktree, not proof about this one.
